route:
  match:
  routes:
    - match:
        - receiver: "alertmanager"
          type: "Warning"
          minCount: 10
    - match:
        - receiver: "alertmanager"
          type: "Normal"
          message: ".*?above target$"
    - match:
        - receiver: "alertmanager"
          type: "Normal"
          message: ".*?below target$"
receiverConfigs:
  - name: alertmanager
    config:
      host: 10.49.18.83:9093
